package com.epam.esm.service.comparator;

import com.epam.esm.entity.GiftCertificate;

import java.util.Comparator;

public class GiftComparatorByNameDESC implements Comparator<GiftCertificate> {
    @Override
    public int compare(GiftCertificate o1, GiftCertificate o2) {
        return 0;
    }
}
